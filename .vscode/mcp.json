{
	"servers": {
		"odoo": {
			"type": "stdio",
			"command": "${workspaceFolder}/.venv/bin/odoo-fast-mcp",
			"args": [
				"--config",
				"${workspaceFolder}/config.json"
			]
		}
	},
	"inputs": []
}